#include <stdio.h>

void lectura_matriz(...);
void proceso(...);
void resultado(...);

int main(){
    // Declarar variables a utilizar
    lectura_matriz(...);
    proceso(...);
    resultado(...));
    return 0;
}